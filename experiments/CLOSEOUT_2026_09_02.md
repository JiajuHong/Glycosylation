# 项目收尾记录：2026-09-02

## 后续范围调整：现用版本必须包含条件

按用户要求，现用第三层已移除无条件训练选项，传统基线已移除纯结构组。
传统基线现保留结构+条件、纯条件两组，共18次已有运行；没有重新训练或改变其指标。
去条件实验的运行器、结果、日志和检查点已移至
`artifacts/archives/without_conditions_20260902/` 下的原相对路径；
变更前的源码与完整基线表保存在该目录的 `pre_removal.tar.gz`，可恢复。
下文去条件实验描述及命令属于历史记录，不是现用功能。
保留条件是任务范围选择，不撤销既有消融结果，也不构成条件性能增益的证明。
XGBoost实验中的deep_only/deep_structure仍含冻结L3的条件表示，不属于无条件模型。

本次范围调整后的服务器验证：17项相关测试通过；两组基线均含同一套条件特征，
保留的18行基线指标与归档原表逐项一致。冻结主模型seed 0的477条验证/测试预测
分类全部不变，最大Beta分数差为 `2.980232238769531e-07`；62项模型清单哈希通过。
未重新训练模型，未运行无关实验。

## 初次收尾修正（历史记录）

- 正式工作总结的时间外推基线旧数字已替换为当前非手性 Morgan 结果：
  structure-only XGBoost Macro-F1=0.817532，AUROC=0.912679。
- XGBoost 分类头和混合模型明确标记为实验比较：只保存指标及逐样本预测，
  不保存分类器、不接入三层推理。正式模型仍是冻结的 Tri-L3 + MLP。
- 第二层按当前模型清单定义为文献证据检索，不再称为模型适用域。

## 去条件消融（已完成并归档的历史记录）

新增训练参数 `--without-conditions`，只对 `crossattn_tri` 生效，默认关闭。
完全移除条件编码器与四个条件 token，保留两侧全局表示与三个局部/交互表示。
L3 分类输入由 1152 维变为 640 维，不是只在推理时把条件清零。

- 数据：`glyco_model_local.csv`，相同 `split_pair_group`。
- 种子：0、1、2；batch size=32，最多200轮。
- Chiral-GINE稳定训练配置、损失函数、阈值选择规则与主模型相同。
- 无条件模型从头训练；完整条件模型复用已经精确复现的正式三种子结果。
- 参数量及随机初始化路径会变化，不宣称这是参数量匹配实验。
- 在验证集早停/选择阈值，测试集只做最终评价；不根据结果追加调参。

运行：

```bash
python -m experiments.run_condition_ablation_v1 --gpus 0 1
```

结果与状态：`results/condition_ablation_v1/`。
训练日志：`logs/condition_ablation_v1/`。
检查点：`artifacts/checkpoints/condition_ablation_v1/`。
只有 `run_manifest.json` 中 `status=completed` 才代表该补充实验完成。

本次已完成：服务器运行清单记录完成时间为2026-09-02 15:55:16（北京时间），
3/3种子训练及逐样本预测导出完成，结果、检查点和日志已同步回本地。

| 配置 | 测试 Macro-F1（均值 ± SD） | 测试 Accuracy（均值） | 测试 AUROC（均值） |
|---|---:|---:|---:|
| 完整条件 L3 | 0.939112 ± 0.007214 | 0.941176 | 0.969459 |
| 去条件 L3 | 0.950299 ± 0.006737 | 0.952381 | 0.971538 |

去条件平均Macro-F1高1.12个百分点，当前结果不支持“条件模块提高分类性能”的结论。
种子0、1、2的精确McNemar检验p值分别为0.226563、0.454498、1.000000，
均未达到0.05；该检验比较配对分类正确性，不是Macro-F1差异的显著性检验。
该消融仅覆盖供受体对分组切分，且非参数量匹配，不能据此断言条件普遍无用，
也不能证明模型有可靠的条件优化能力。不依据本次测试结果追加调参或自动替换冻结主模型。
数值见 `summary.csv`，逐种子比较见 `paired_comparisons.csv`。

## 版本与证据边界

本记录所在的收尾提交选择性记录相关源码、实验协议、说明及小型模型清单/验收记录，
包括第二层旧实现的移除；不提交无关原始CSV删除、模型权重、归档压缩包或大型结果表。
可用 `git log -1 -- experiments/CLOSEOUT_2026_09_02.md` 定位该源码版本。
工作区仍可能保留未提交的历史数据变化和实验产物，不宣称整个工作树已清洁。
独立源码快照、SHA-256清单和真实Git状态用于关联外部保存的实验产物。
原始模型检查点保持不变；新增实验不替换生产模型。

已知手性感知Morgan曾得到更强结果，删除其产物不构成该结果无效的证据。
当前非手性基线只支持与普通Morgan比较，不能据此宣称优于所有传统手性感知方法。

## 初次收尾必要验证（历史记录）

- 服务器 `python -m unittest layer3.tests.test_chiral_gine -q`：17项通过。
- 新增无条件模型能在完全不提供条件字段时前向/反向；默认完整条件模型兼容原状态字典。
- 冻结seed 0在477条验证/测试反应上的分类全部不变，最大Beta分数差为
  `4.172325134277344e-07`（导出批量不同引起的浮点误差范围）。
- 更新后的生产模型清单62项哈希验证通过，见 `artifacts/audit/closeout_v1.json`。
- 未重复执行全部端到端验收，未重训原主模型。

源码快照生成：`python -m experiments.archive_closeout_v1`。
输出为 `artifacts/releases/closeout_20260902/`，包含源码压缩包、逐文件哈希、
真实Git状态和数据哈希；实验完成依据为运行清单及结果，而不是源码快照本身。
